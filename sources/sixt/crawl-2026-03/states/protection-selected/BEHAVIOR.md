# Protection card selection

By default, none of the protection cards is selected + primary CTA (`Continue`) is disabled.

Users can click `Continue` and proceed to add-ons only when one of 4 protection cards is selected.

On mobile screens, protection cards appear as accordions and expand when selected. However, users can tap precisely on disclosure chevron to reveal hidden info without selecting protection card.